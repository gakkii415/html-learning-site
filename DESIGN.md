# Design

<!-- repository-creator:managed:start -->

## Role

ユーザーがどう見て、理解し、操作するかを扱う。機能の振る舞いは `FEATURES.md`、伝える情報や文言は `CONTENT.md` の責務とし、ここでは重複して定義しない。

## Goal

そのプロダクト固有の目的・利用者・内容に合った、意図のあるプロダクトレベルの UI / UX を作る。AIが生成しがちな画一的な「それっぽいUI」を避ける。

## Method

- まずプロダクト、対象ユーザー、主要タスク、現在のUIを理解する。
- 既存UIがある場合は良い部分を維持し、全面刷新をデフォルトにしない。
- 判断材料が必要な場合は、ユーザーに探させず、実在する複数の参考プロダクトを自分で調査する。
- UIパターン、アクセシビリティ、状態設計などに根拠が必要な場合は、プロダクトに適した信頼性の高い公式ガイドラインを選んで参照する。ブランド表現を模倣する目的では使わない。
- 参考先はそのまま模倣せず、このプロダクトに有効な設計判断だけを抽出する。
- 実装前に候補を比較し、明確な改善効果があるものだけを採用する。
- 使いやすさ、情報階層、アクセシビリティ、既存UIとの整合性、モバイル操作を装飾より優先する。
- UI変更後は、利用可能なら実画面を確認する。必要に応じてスマートフォン幅とPC幅を確認し、問題があれば修正する。利用できない場合は、その工程だけを省略する。
- 追加変更の利益が小さくなったら停止する。変更しない判断も正しい結果として扱う。

## Avoid Generic AI Defaults

明確なプロダクト上の理由がない限り、不要なカードやコンテナ、過剰な角丸・pill・badge、紫青系グラデーション、glass・glow、巨大Heroや見出し、均質なカードグリッド、UIライブラリのデフォルト感、アイコン過多、genericなSaaSコピー、意味の薄い装飾やアニメーションをデフォルト採用しない。

## References

- 継続的に価値がある参考画像・PDF・スクリーンショットだけを `design-reference/` に保存する。必要になるまでフォルダは作らない。
- 一時的な調査資料はリポジトリに残さない。
- 採用した参考URLと、長く維持すべき設計判断だけを下の `Project Direction` に簡潔に残す。

<!-- repository-creator:managed:end -->

## Project Direction

初回のUI作業時にAIが記入し、その後は恒常的な方向性が変わった場合だけ更新する。

- Product / users / primary task: HTML初心者が、迷わずコードを書き、同じ画面で結果と合否を確かめる。
- Desired impression: 学校のプリントではなく、道具を実際に触る小さな制作スタジオ。明快で、少し手作業の温度がある。
- Reference products / what to borrow: コードエディタの情報密度、ノートの書き込み感、ブラウザの出力面を役割ごとに分ける。特定ブランドの外観は模倣しない。
- What to avoid: ダッシュボード型カード一覧、紫系グラデーション、装飾目的のアイコン、巨大な宣伝Hero、過剰な角丸。
- Durable design decisions: 青みの紙色と濃いインクを基調に、朱色を操作・現在地、マスタードを学習の要点に使う。左のタグ列を進行軸にし、デスクトップはコードとプレビューを常時並置、モバイルはタブ切り替えにする。
