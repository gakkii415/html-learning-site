# Features

<!-- repository-creator:managed:start -->

## Role

プロダクトが何をするかと、その機能をどこまで充実させるかを扱う。コア機能、機能拡充、ユーザーフロー、状態、業務ルール、権限、データ、外部連携の振る舞いを対象とする。コンテンツは `CONTENT.md`、見せ方や操作表現は `DESIGN.md` の責務とし、ここでは重複して定義しない。

## Goal

初回はコア機能へ集中して中心価値を高品質に成立させ、その後は現在の完成度を基準に必要な機能群をまとめて拡充し、継続的な実利用に耐えるプロダクトレベルへ育てる。最初から全部を作ることも、一機能ずつ細切れに増やすことも目的にしない。

## Working Model

まずプロダクト、対象ユーザー、中心価値、現在の実装と完成度を理解し、今回の依頼がコアの確立か、既存プロダクトの拡充かを判断する。

### Core Establishment

- 初回作成では、中心価値を実際に体験できるコア機能群へ集中する。
- 複数の周辺機能へ広く着手せず、主要フローを入口から結果まで完成させる。
- コア体験に不可欠な保存、空、失敗、取消・復帰などは、周辺機能ではなくコアの完成条件として扱う。
- プロダクトが成立したと判断できるところまで作り、見かけ上の充実のために低優先機能へ拡散しない。

### Product Expansion

- 拡充時は、現在の機能一覧だけでなく、初回利用から継続利用までのユーザーフロー全体を確認する。
- 必要なら実在する類似・隣接プロダクトを調査し、重大な欠落、利用を中断させる不足、継続利用に必要な能力を洗い出す。
- まず次に目指す完成状態を定め、その状態に必要な機能群を今回の `Expansion Set` として選ぶ。
- `Expansion Set` は一機能や一テーマに限定しない。次の完成状態へ意味のある進歩を生むなら、関連する複数の機能や異なる領域の高優先項目をまとめて扱ってよい。
- 件数を機械的に制限しない。AIが一度に高品質で実装・統合・検証できる範囲を基準にする。
- 既存機能との接続、データ、状態、失敗時の振る舞いまで含めて完成させ、独立した機能の寄せ集めにしない。
- 今回の完成状態に不要な低優先項目、価値が不確かな項目、別プロダクトに近い大規模領域だけを見送る。

## Verification and Records

- 実装前に今回の完成状態と完了条件を明確にし、実装後は主要フローと重要な失敗系をリスクに応じた方法で確認する。
- 既存の利用中の振る舞いとデータを尊重し、互換性を壊す変更や全面刷新をデフォルトにしない。
- コード、API仕様、Issue、テストで十分に表現される詳細は重複して書かない。現在の成熟度、次の完成状態、今回の拡充範囲、長く維持すべき判断だけを下に残す。
- 今回設定した完成状態へ到達し、追加による価値より複雑化が大きくなったら、その拡充サイクルを終了する。

<!-- repository-creator:managed:end -->

## Project Feature Maturity

初回の機能設計時にAIが記入し、その後は完成段階や恒常的な方針が変わった場合だけ更新する。

- Product / users / core outcome:
- Current maturity:
- Core feature set / completion criteria:
- Next target state:
- Expansion Set:
- Deferred lower-priority items / reasons:
- Important flows / states / data decisions:
- Durable feature decisions:
